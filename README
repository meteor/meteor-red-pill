If you actually changed the script, update VERSION to a random text-character
hex string here, and update release.json to match. (release.json! Not the other
files you have sitting around still)

Then:

$ VERSION=a9f7d8d731 RELEASE_VERSION=0.9.0-rc12 ./push.sh
