If you actually changed the script, update VERSION to a random ten-character
hex string here, and update release.json to match. (release.json! Not the other
files you have sitting around still)

Then:

$ VERSION=381da7e862 RELEASE_VERSION=1.0-rc.4 ./push.sh
